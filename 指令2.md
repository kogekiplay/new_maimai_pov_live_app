/plan 
重要!!一定要多向我发起提问,我需要确认你理解我的需求,你需要提问收集足够的信息,根据我的需求,设计出符合我需求的方案

我使用的是Windows环境开发,无法看到调试信息,有一个浮窗能让我看到调试信息,以便于开发过程中定位问题

我使用github action进行编译,请你完成编码后推送,并且监控编译过程,如果有报错就进行修复,需要等待你就运行个sleep进行等待

**编译通过后，指引我按照**【人工验证标准】在真机上跑一次,并发起提问收集我的测试反馈。测试通过了，再进入下一阶段。否着不可以终止输出.

<br />

一切基于事实,不要猜测,随时可以调用提问工具问我,请先收集足够的信息,再开始计划

我已经开发完成了目前的视频实时处理管线,接下来需要添加推流功能
我是如下这样计划的,请你先阅读目前的app代码,理解好目前的视频实时处理管线,分析如下的方案是否合适,检查是否有需要补充的设计.
然后根据我的需求,设计出符合我需求的方案

核心战略：
我们不要去手写 VTCompressionSession (H.264) 和 AudioConverter (AAC) 以及手拼 FLV 协议包（那至少需要 2000 行枯燥且极易出错的 C/Swift 代码）。
我们将引入知名的开源库 HaishinKit，但绝对不使用它自带的相机和麦克风采集模块。我们只把它当成一个“高级编码打包发送器”。只要我们把当前的 CVPixelBuffer 包装成系统标准的 CMSampleBuffer 塞给它，它就会自动调用苹果底层的硬件编码器并推流。

为了保证安全，我们依然采用“极小步幅 Vibe Coding”，请按以下 4 个阶段进行：

阶段一：引入依赖与创建推流引擎空壳
目标：把推流的网络和状态管理剥离在一个独立的类中。

【前期准备】：
请先通过 Swift Package Manager (SPM) 在项目中添加 HaishinKit 依赖：https://github.com/shogo4405/HaishinKit.swift (选择最新版本)。

【提供给 AI 的 Prompt】：

"请帮我在项目中新建一个类 RTMPStreamManager: ObservableObject，并引入 HaishinKit。

这个类负责管理 RTMP 连接，内部需要实例化 RTMPConnection 和 RTMPStream。

暴露 @Published var isStreaming: Bool = false 和 @Published var streamStatus: String = ""。

提供 func startPublish(url: String, streamKey: String) 和 func stopPublish() 方法。

配置 RTMPStream 的视频参数：1080x1920 (竖屏，因为我们的输出是 720x1280 甚至更高，请匹配 Config.outputWidth/Height)，FPS 设为 60，码率设为 4000 kbps，Profile Level 设为 Main。音频配置为 44100Hz, 128kbps, AAC。

监听 RTMPConnection 的状态回调，在连接成功时自动调用 stream.publish(streamKey)，并更新 streamStatus。
注意：只写这个管理类，先不要碰项目里的任何其他代码。"

测试：编译通过即可，暂时没有界面。

阶段二：打通数据桥梁 (The Bridge)
目标：HaishinKit 只吃 CMSampleBuffer，但我们的视频现在是 CVPixelBuffer，需要写一个转换工具。

【提供给 AI 的 Prompt】：

"现在需要在 LivePipelineManager 和 RTMPStreamManager 之间建立数据桥梁。

请在 RTMPStreamManager 中提供两个供外部调用的方法：
func appendVideo(pixelBuffer: CVPixelBuffer, timestamp: CMTime)
func appendAudio(sampleBuffer: CMSampleBuffer)

在 appendVideo 方法中，你需要使用 CoreMedia 的 API，将传入的 CVPixelBuffer 和 timestamp 包装成标准的 CMSampleBuffer（需要先创建 CMVideoFormatDescription，再创建 CMSampleTimingInfo，最后 CMSampleBufferCreateForImageBuffer）。

将生成的视频 CMSampleBuffer 和直接传入的音频 CMSampleBuffer，分别调用 RTMPStream 的 appendSampleBuffer(_:withType: .video) 和 appendSampleBuffer(_:withType: .audio) 喂给 HaishinKit。
注意：转换 CVPixelBuffer 到 CMSampleBuffer 时，请注意复用 CMVideoFormatDescription 以节省性能，不要每帧都重新创建 FormatDesc。"

测试：编译通过即可。

阶段三：将管线接入推流器
目标：把第一部分产生的数据，源源不断地倒进第二部分的推流器中。

【提供给 AI 的 Prompt】：

"现在我们要把管线和推流器连接起来。

在 LivePipelineManager 中，持有一个 RTMPStreamManager 的实例：let streamManager = RTMPStreamManager()。

找到 LivePipelineManager 中 onStreamBufferAvailable 被调用的地方（在 pipelineQueue 里），不要只通过闭包外传了，直接在这里调用 self.streamManager.appendVideo(pixelBuffer: pixelBuffer, timestamp: timestamp)。

找到 camera.onAudioSample 回调的地方，直接调用 self.streamManager.appendAudio(sampleBuffer: sample)。

为了解决音频和视频可能存在的微小时间戳漂移，请确保音频和视频的 CMTime 都是基于 ProcessInfo.processInfo.systemUptime 或相机的时钟计算的绝对时间（你的 CameraCaptureManager 里已经有了 audioClockOffset 和 videoClockOffset 的逻辑，请确保这里是对齐的）。"

测试：运行 App，确保不推流的时候，单纯跑这些数据搬运逻辑不会引起 Crash 或严重掉帧。

阶段四：UI 界面实装与实测
目标：加上按钮，开始真正的直播！

【提供给 AI 的 Prompt】：

"最后一步，我们需要在 UI 上添加推流控制。

在 Phase2View 的控制面板 (controlCard) 中，增加一个『Live Stream』的分组。

里面包含两个 TextField，分别用于输入 RTMP URL 和 Stream Key（可以用 @AppStorage 保存这两个值以便下次测试使用）。

增加一个大大的 推流/停止 按钮。绑定 pipeline.streamManager.startPublish 和 stopPublish。

在界面上找个地方显示 pipeline.streamManager.streamStatus 以便我能看到连接状态（如 Connecting, Connected, Failed 等）。"